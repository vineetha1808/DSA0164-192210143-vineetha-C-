#include<iostream>
#include<cmath>
using namespace std;
int main()
{
	int num,sqrt_num;
	cout<<"enter a number:";
	cin>>num;
	sqrt_num=sqrt(num);
	if(num<0)
	{
		cout<<"square root of a number is not real"<<sqrt(num);
	}
	else
	{
		cout<<"square root of a number is:"<<sqrt(num);
	}
	return 0;
}
